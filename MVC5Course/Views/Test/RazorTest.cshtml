@model int[]


@for (int i = 0; i < Model.Length; i++)
{
    @Model[i];
}

@{
    for (int i = 0; i < Model.Length; i++)
    {
        @:@Model[i]
    }
}

@{
    @:<ul>
    for (int i = 0; i < Model.Length; i++)
    {
        <text>  <ui>@(Model[i])@"</ui>
        </text>

    }
    @:
</ul>
}
